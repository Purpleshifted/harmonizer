/**
 * Config for wave effect worklet (loop buffer path).
 * WaveBufferCache generates a buffer in-memory if no file is used.
 */
export const WAVE_SAMPLER_CONFIG = {
    path: '/samples/wave/loop.wav',
};
